---
phase: 02-high-priority-new-tools
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/index.ts
autonomous: true
requirements: [CF-01, CF-02, CF-03, CF-04, CF-05, CF-06, COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08, COMP-09, COMP-10]
must_haves:
  truths:
    - "New custom field tools are registered and available to MCP clients"
    - "New compliance tools are registered and available to MCP clients"
    - "Full build passes with zero TypeScript errors"
    - "Full test suite passes with no regressions"
  artifacts:
    - path: "src/index.ts"
      provides: "Registration of new tool files"
      contains: "registerCustomFieldTools"
  key_links:
    - from: "src/index.ts"
      to: "src/tools/custom-fields.ts"
      via: "import and register"
      pattern: "registerCustomFieldTools"
    - from: "src/index.ts"
      to: "src/tools/compliance.ts"
      via: "import and register"
      pattern: "registerComplianceTools"
---

<objective>
Wire the two new tool files (custom-fields.ts, compliance.ts) into the server entry point and run full integration verification.

Purpose: Plans 01 and 02 created the tool files but they are not yet registered with the MCP server. This plan connects them and verifies the full system works.
Output: Updated src/index.ts with imports and registrations
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.ts
@src/tools/custom-fields.ts
@src/tools/compliance.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register custom-fields and compliance tools in index.ts</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (full file — find import section and registration block)
    - src/tools/custom-fields.ts (verify export name: registerCustomFieldTools)
    - src/tools/compliance.ts (verify export name: registerComplianceTools)
  </read_first>
  <action>
**Step 1: Add imports to src/index.ts**

Add these two imports after the existing tool imports (after `import { registerWebhookTools } from "./tools/webhooks.js";` at line 22):

```typescript
import { registerCustomFieldTools } from "./tools/custom-fields.js";
import { registerComplianceTools } from "./tools/compliance.js";
```

**Step 2: Add registration calls**

Add these two calls after `registerSupplementaryTools(server, apiClient);` (at line 147):

```typescript
registerCustomFieldTools(server, apiClient);
registerComplianceTools(server, apiClient);
```

**Step 3: Run full build and test suite**

After making changes, verify:
1. `npm run build` — zero TypeScript errors
2. `npm test` — all tests pass (existing + new custom-fields + new compliance + new PO + new invoice tests)
  </action>
  <verify>
    <automated>cd /Users/przbadu/projects/pex/procurementexpress-mcp && npm run build && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "registerCustomFieldTools" src/index.ts returns 2 matches (import + call)
    - grep "registerComplianceTools" src/index.ts returns 2 matches (import + call)
    - grep 'from "./tools/custom-fields.js"' src/index.ts returns 1 match
    - grep 'from "./tools/compliance.js"' src/index.ts returns 1 match
    - npm run build exits with code 0
    - npm test exits with code 0 and reports 0 failures
  </acceptance_criteria>
  <done>Both new tool files registered in index.ts. Full build passes. Full test suite passes with no regressions. All 23 requirement tools are available to MCP clients.</done>
</task>

</tasks>

<verification>
- `npm run build` — zero TypeScript errors
- `npm test` — all tests pass
- `grep -c "registerTool" src/tools/custom-fields.ts` returns 6
- `grep -c "registerTool" src/tools/compliance.ts` returns 10
- `grep "registerCustomFieldTools\|registerComplianceTools" src/index.ts` returns 4 matches (2 imports + 2 calls)
</verification>

<success_criteria>
- src/index.ts imports and registers both registerCustomFieldTools and registerComplianceTools
- npm run build passes with zero errors
- npm test passes all tests with zero failures
- MCP server would expose all 23 new tools (6 CF + 10 COMP + 4 PO + 2 INV + 1 existing INV-03) when started
</success_criteria>

<output>
After completion, create `.planning/phases/02-high-priority-new-tools/02-05-SUMMARY.md`
</output>
