---
phase: 04-low-priority-new-tools
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/index.ts
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-03
  - POL-04
  - POL-05
  - POL-06
  - LOW-01
  - LOW-02
  - LOW-03
  - LOW-04
  - LOW-05
  - LOW-06

must_haves:
  truths:
    - "Policy tools are callable via the MCP server"
    - "Digital invoice tool is callable via the MCP server"
    - "Chat message tools are registered ONLY when API version is V3"
    - "SAM.gov and supplier approval tools are callable (already wired via registerSupplierTools)"
    - "npm run build succeeds and npm test passes with all new tools integrated"
  artifacts:
    - path: "src/index.ts"
      provides: "Import and registration of registerPolicyTools, registerDigitalInvoiceTools, registerChatMessageTools (V3 gated)"
      contains: "registerPolicyTools"
  key_links:
    - from: "src/index.ts"
      to: "src/tools/policies.ts"
      via: "import registerPolicyTools"
      pattern: "registerPolicyTools"
    - from: "src/index.ts"
      to: "src/tools/digital-invoices.ts"
      via: "import registerDigitalInvoiceTools"
      pattern: "registerDigitalInvoiceTools"
    - from: "src/index.ts"
      to: "src/tools/chat-messages.ts"
      via: "import registerChatMessageTools inside else block"
      pattern: "registerChatMessageTools"
---

<objective>
Wire all new Phase 4 tool files into the MCP server entry point and verify full integration.

Purpose: Plans 01-03 created the tool files but they are not yet imported or registered in index.ts. This plan wires them in, with chat messages V3-gated, and runs the full verification suite.

Output: Updated `src/index.ts` with all new imports and registrations. Full build + test pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/index.ts
@src/tools/policies.ts
@src/tools/digital-invoices.ts
@src/tools/chat-messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire new tool files into index.ts with V3-gated chat messages</name>
  <files>src/index.ts</files>
  <read_first>src/index.ts, src/tools/policies.ts, src/tools/digital-invoices.ts, src/tools/chat-messages.ts</read_first>
  <action>
**Step 1: Add imports at the top of src/index.ts**

Add these 3 imports alongside the existing tool imports (after the `registerWebhookTools` import):

```typescript
import { registerPolicyTools } from "./tools/policies.js";
import { registerDigitalInvoiceTools } from "./tools/digital-invoices.js";
import { registerChatMessageTools } from "./tools/chat-messages.js";
```

**Step 2: Register non-gated tools**

Add after the existing `registerUploadTools(server, apiClient);` line (in the ungated section):

```typescript
registerPolicyTools(server, apiClient);
registerDigitalInvoiceTools(server, apiClient);
```

Note: SAM.gov and supplier approval tools are already wired because they were added inside `registerSupplierTools` in Plan 02.

**Step 3: Register V3-only chat message tools**

Add `registerChatMessageTools` INSIDE the `else` block of the `if (isV1)` auth branching. This block starts around line 82 (`} else {`). Add the registration call inside that else block, after the V3 authenticate tool registration but before the closing brace of the else block:

```typescript
} else {
  // V3: OAuth2 authentication
  server.registerTool("authenticate", { ... }, ...);

  // V3-only tools
  registerChatMessageTools(server, apiClient);
}
```

CRITICAL: Chat messages MUST be inside the `else` (V3) block. They must NOT be in the ungated section. This mirrors the pattern where V3-only auth tools are registered conditionally.

**Step 4: Verify build and tests**

Run `npm run build` and `npm test` to confirm everything compiles and all tests pass.
  </action>
  <verify>
    <automated>cd /Users/przbadu/projects/pex/procurementexpress-mcp && npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "registerPolicyTools" src/index.ts` matches (import and call)
    - `grep "registerDigitalInvoiceTools" src/index.ts` matches (import and call)
    - `grep "registerChatMessageTools" src/index.ts` matches (import and call)
    - Chat message registration is inside the else (V3) block, NOT in the ungated section
    - `npm run build` succeeds with zero errors
    - `npm test` passes all tests including new ones from Plans 01-03
  </acceptance_criteria>
  <done>All Phase 4 tools wired into index.ts. Chat messages V3-gated. Build succeeds. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `npm run build` — zero TypeScript errors, dist/ updated
- `npm test` — full suite passes
- `grep -c "registerTool" src/tools/policies.ts` — 6 policy tools
- `grep -c "registerTool" src/tools/chat-messages.ts` — 3 chat tools
- `grep -c "registerTool" src/tools/digital-invoices.ts` — 1 digital invoice tool
- Chat messages registered inside V3 else block in index.ts
</verification>

<success_criteria>
- src/index.ts imports and registers all new tool files
- registerChatMessageTools is inside the `else` (V3-only) block
- registerPolicyTools and registerDigitalInvoiceTools are in the ungated section
- `npm run build` succeeds
- `npm test` passes all tests (new and existing)
</success_criteria>

<output>
After completion, create `.planning/phases/04-low-priority-new-tools/04-04-SUMMARY.md`
</output>
