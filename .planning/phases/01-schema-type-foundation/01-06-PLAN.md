---
phase: 01-schema-type-foundation
plan: 06
type: execute
wave: 3
depends_on: ["01-03", "01-04", "01-05"]
files_modified: []
autonomous: false

requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05
  - SCHEMA-06
  - SCHEMA-07
  - SCHEMA-08
  - SCHEMA-09
  - SCHEMA-10
  - SCHEMA-11
  - SCHEMA-12
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
  - TYPE-05
  - TYPE-06
  - TYPE-07
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "npm run build passes with zero TypeScript errors"
    - "npm test passes all tests with no regressions"
    - "No inline customFieldValueSchema remains in any tool file"
    - "All list tools use Summary types"
    - "Error handler correctly parses all Rails error formats"
  artifacts:
    - path: "src/schemas.ts"
      provides: "Shared schemas used by all tool files"
      exports: ["customFieldValueSchema", "lineItemSchema", "invoiceLineItemSchema"]
  key_links:
    - from: "src/tools/purchase-orders.ts"
      to: "src/schemas.ts"
      via: "import"
      pattern: "from.*schemas\\.js"
    - from: "src/tools/invoices.ts"
      to: "src/schemas.ts"
      via: "import"
      pattern: "from.*schemas\\.js"
---

<objective>
Run full integration verification across all Phase 1 changes. Confirm build passes, all tests pass, and all phase success criteria are met.

Purpose: Final gate before marking Phase 1 complete. Catches any cross-plan integration issues.

Output: Verified clean build and test suite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-type-foundation/CONTEXT.md
@.planning/phases/01-schema-type-foundation/01-03-SUMMARY.md
@.planning/phases/01-schema-type-foundation/01-04-SUMMARY.md
@.planning/phases/01-schema-type-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and test verification</name>
  <files></files>
  <read_first>
    - src/api-client.ts (verify error handler fix is in place)
    - src/schemas.ts (verify shared schemas exist)
    - src/types.ts (verify WebhookSummary, ApprovalFlowSummary exist)
  </read_first>
  <action>
Run full verification suite:

1. `npm run build` — must exit 0 with zero TypeScript errors
2. `npm test` — must pass all tests with zero failures
3. Verify no inline `customFieldValueSchema` remains in tool files:
   - `grep -r "const customFieldValueSchema" src/tools/` should return no results
4. Verify all list tools use Summary types:
   - `grep "PurchaseOrderSummary" src/tools/purchase-orders.ts` should match
   - `grep "InvoiceSummary" src/tools/invoices.ts` should match
   - `grep "WebhookSummary" src/tools/webhooks.ts` should match
   - `grep "ApprovalFlowSummary" src/tools/approval-flows.ts` should match
   - `grep "Company\[\]" src/tools/companies.ts` should match for list (not CompanyDetail[])
5. Verify error handler:
   - `grep "errorBody.error" src/api-client.ts` should match
6. Verify shared schema imports:
   - `grep 'from.*schemas\.js' src/tools/purchase-orders.ts` should match
   - `grep 'from.*schemas\.js' src/tools/invoices.ts` should match

If any check fails, fix the issue before proceeding.
  </action>
  <verify>
    <automated>cd /Users/przbadu/projects/pex/procurementexpress-mcp && npm run build && npm test && echo "PHASE 1 VERIFICATION PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm test` exits 0 with all tests passing
    - `grep -r "const customFieldValueSchema" src/tools/` returns empty (no inline schemas)
    - `grep "PurchaseOrderSummary" src/tools/purchase-orders.ts` returns a match
    - `grep "InvoiceSummary" src/tools/invoices.ts` returns a match
    - `grep "WebhookSummary" src/tools/webhooks.ts` returns a match
    - `grep "ApprovalFlowSummary" src/tools/approval-flows.ts` returns a match
    - `grep "errorBody.error" src/api-client.ts` returns a match
  </acceptance_criteria>
  <done>Full build passes with zero errors. All tests pass. All phase success criteria verified via grep checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of Phase 1 changes</name>
  <files></files>
  <action>
Present the completed Phase 1 changes to the user for review. The user will verify the overall quality of changes by inspecting the git diff and running build/test commands.

What was built:
- Fixed error handler to parse all Rails error formats ({error}, {errors}, {message})
- Extracted shared Zod schemas to src/schemas.ts (customFieldValueSchema, lineItemSchema, invoiceLineItemSchema, nestedDestroyMixin)
- Created WebhookSummary, ApprovalFlowSummary, ApprovalFlowVersion types in src/types.ts
- Added missing serializer fields to PurchaseOrderSummary, PurchaseOrder, InvoiceSummary, Invoice, CompanyDetail
- Wired Summary types to all list tools (PO, Invoice, Webhook, ApprovalFlow, Company)
- Added missing optional params per Rails strong params audit (validation_date, xero_id, archived, etc.)

How to verify:
1. Run `npm run build` — should exit 0 with no errors
2. Run `npm test` — should pass all tests
3. Spot-check: `grep -c "const customFieldValueSchema" src/tools/*.ts` — should be 0 for all files
4. Spot-check: Open `src/schemas.ts` and verify it exports customFieldValueSchema, lineItemSchema, invoiceLineItemSchema
5. Spot-check: Open `src/api-client.ts` and verify error handler checks `errorBody.error` first
6. Review git diff for any unintended changes
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human approval"</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" after reviewing changes
  </acceptance_criteria>
  <done>User has reviewed and approved all Phase 1 changes.</done>
</task>

</tasks>

<verification>
- `npm run build && npm test` both pass
- All grep checks confirm correct wiring
- Human review of overall changes
</verification>

<success_criteria>
1. Zero TypeScript errors in build
2. All existing tests pass (no regressions)
3. No inline schema duplication remains
4. All list tools use correct Summary types
5. Error handler parses all Rails error formats
6. Human approval of overall Phase 1 changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-type-foundation/01-06-SUMMARY.md`
</output>
